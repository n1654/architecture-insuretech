apiVersion: apps/v1
kind: Deployment
metadata:
  name: insuretech-app-deployment
  namespace: insuretech
  labels:
    app: scaletestapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scaletestapp
  template:
    metadata:
      labels:
        app: scaletestapp
    spec:
      containers:
      - name: scaletestapp
        image: shestera/scaletestapp
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "10Mi"
          limits:
            memory: "30Mi"